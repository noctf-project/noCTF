export enum UserFlag {
  VALID_EMAIL = "valid_email",
  ADMIN = "admin",
  BLOCKED = "blocked",
  HIDDEN = "hidden",
}

export enum TeamFlag {
  BLOCKED = "blocked",
  HIDDEN = "hidden",
  FROZEN = "frozen",
}
export enum ActorType {
  USER = "user",
  ROLE = "role",
  SYSTEM = "sys",
  TEAM = "team",
}
