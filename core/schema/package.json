{
  "name": "@noctf/schema",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "kysely-codegen --url 'env(POSTGRES_URL)' --out-file dist/index.d.ts",
    "release": "pnpm run build",
    "dev": "echo 'Nothing to do' && exit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "kysely": "catalog:"
  },
  "devDependencies": {
    "kysely-codegen": "^0.16.8",
    "typescript": "catalog:"
  },
  "type": "module",
  "types": "./dist/index.d.ts"
}
